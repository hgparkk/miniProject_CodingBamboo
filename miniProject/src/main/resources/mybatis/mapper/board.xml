<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codingbamboo.miniproject.board.dao.IBoardDAO">
	<!-- q&a 목록 가져오기 -->
	<select id="getBoardList" resultType="com.codingbamboo.miniproject.board.dto.BoardDTO" parameterType="com.codingbamboo.miniproject.common.SearchVO">
		SELECT
			a.qu_no
			, a.user_id
			, b.user_name
			, a.qu_title
			, a.qu_content
			, a.qu_isread
		FROM
			question a, user_info b
		WHERE
			a.user_id = b.user_id
			ORDER BY qu_no DESC
	</select>
	
	<!-- q&a에 질문 추가 -->
	<insert id="insertBoard" parameterType="com.codingbamboo.miniproject.board.dto.BoardDTO">
		INSERT INTO question(
			qu_no
			, user_id
			, qu_title
			, qu_content
			, qu_isread
		) VALUES (
			qu_seq.NEXTVAL
			, <!-- #{userId} --> 'qwe123'
			, #{quTitle}
			, #{quContent}
			, 0
		)
	</insert>
	
	<select id="getBoardCount" resultType="int">
		SELECT COUNT(*)
		FROM question a, user_info b
		WHERE 1=1
		  AND a.user_id = b.user_id(+)
	</select>
	
	<select id="getBoard" resultType="com.codingbamboo.miniproject.board.dto.BoardDTO">
		SELECT 
		      a.qu_no
		    , a.qu_title 
		    , a.user_id
		    , b.user_name
		    , a.qu_content 
		FROM question a, user_info b
		WHERE 1=1
		  AND a.user_id = b.user_id(+)
		  AND a.qu_no = #{no}
	</select>
	
	<delete id="deleteBoard" parameterType="int">
		DELETE FROM question
		WHERE qu_no = #{quNo}
	</delete>
	


</mapper>